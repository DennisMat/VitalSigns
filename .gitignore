# Ignore these files #

*.class

#exclude entire classes folder recursively#
classes/

#bin/#
#we want the apk file to be uploaded hence I'm not excluding the entire bin directory.

bin/res/
bin/classes.dex
bin/resources.ap_